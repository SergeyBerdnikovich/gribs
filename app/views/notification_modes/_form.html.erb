<%= form_for(@notification_mode) do |f| %>
  <% if @notification_mode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notification_mode.errors.count, "error") %> prohibited this notification_mode from being saved:</h2>

      <ul>
      <% @notification_mode.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
